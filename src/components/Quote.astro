---
---

<aside class="sheet section">
	<p class="quote-text">Le prix est fixé à la mission, jamais à l'heure.</p>
</aside>

<style>
	/* The one place the ink is allowed to be large. */
	.quote-text {
		margin: 0;
		font-family: var(--display);
		font-size: clamp(1.5rem, 4.4vw, 3rem);
		font-weight: 400;
		line-height: 1.2;
		letter-spacing: -0.015em;
		color: var(--accent);
		max-width: 22ch;
		text-wrap: balance;
	}

</style>
